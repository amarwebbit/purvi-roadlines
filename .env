# Copy this file to set real credentials. DO NOT commit real secrets to source control.
# Replace the placeholder values below with your real SMTP credentials and recipient email.

# SMTP server host (e.g., smtp.gmail.com for Gmail)
SMTP_HOST=smtp.example.com

# SMTP server port (587 for STARTTLS, 465 for SSL)
SMTP_PORT=587

# Use TLS/SSL? true or false
SMTP_SECURE=false

# SMTP authentication username and password
SMTP_USER=purviroadlines2018@gmail.com
SMTP_PASS=Purvi@2018

# Optional: address to use in the From header. If omitted, SMTP_USER will be used.
FROM_EMAIL=you@yourdomain.com

# The email address which should receive contact form submissions
TO_EMAIL=purviroadlines2018@gmail.com

# Server port (optional)
PORT=3000

# Notes:
# - For Gmail, create an App Password (recommended) and use smtp.gmail.com with port 465 and SMTP_SECURE=true.
# - For SendGrid/Mailgun/SES, use the provider's SMTP host/port and credentials.
# - Keep this file private. Use your hosting provider's environment variable settings in production.
